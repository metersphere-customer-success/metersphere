// 待定
//driver.executeScript("console.log(\"`set speed` is a no-op in code export, use `pause` instead\");");
responseHeaders["${cmdId}"].body = "`set speed` is a no-op in code export, use `pause` instead";
